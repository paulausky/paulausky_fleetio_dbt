name: 'paulausky_fleetio_dbt'
version: '1.0.0'

profile: 'paulausky_fleetio_dbt'  

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  paulausky_fleetio_dbt:
    +materialized: table  # default materialization for models
